spring:
  datasource:
    url: jdbc:mysql://localhost:3306/godo?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

cloud:
  storage:
    endpoint: https://axrksogyxb64.compat.objectstorage.ap-chuncheon-1.oraclecloud.com
    region: ap-chuncheon-1
    access-key: ${ORACLE_ACCESS_KEY}
    secret-key: ${ORACLE_SECRET_KEY}
    bucket: drone-gallery

admin:
  username: admin
  password: ${ADMIN_PASSWORD:godo1234}

logging:
  level:
    com.example.godo: DEBUG
